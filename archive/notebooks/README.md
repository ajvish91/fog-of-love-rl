# Archived notebooks

| Notebook | Purpose |
|----------|---------|
| `get_history.ipynb` | Inspect W&B exports / `pareto_history.json` and derived pickle histories |

Copy exports into `archive/artifacts/` (see that directory’s README). The notebook expects paths relative to `archive/artifacts/`, not the repo root.
